name: Build Windows (MinGW64)

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build-windows:
    runs-on: windows-latest
    defaults:
      run:
        shell: powershell

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup MSYS2 and Install Dependencies
        run: |
          Write-Host "Updating MSYS2..."
          C:\msys64\usr\bin\pacman.exe -Syu --noconfirm
          
          Write-Host "Installing GCC and libsodium..."
          C:\msys64\usr\bin\pacman.exe -S mingw-w64-x86_64-gcc mingw-w64-x86_64-libsodium --noconfirm

      - name: Build DLL and Static Library
        # 关键：将工作目录切换到 src 文件夹
        working-directory: ./src
        run: |
          # 创建 build 文件夹（现在在 src 目录下）
          if (!(Test-Path build)) { New-Item -ItemType Directory -Path build }
          
          Write-Host "Building DLL..."
          # -I../include 表示去上一级目录的 include 文件夹找头文件
          x86_64-w64-mingw32-gcc.exe -shared `
            -I../include `
            -o build/libsodium_crypto.dll `
            libsodium_crypto.c `
            -lsodium

          Write-Host "Building Static Library (.a)..."
          x86_64-w64-mingw32-gcc.exe -c `
            -I../include `
            -o build/libsodium_crypto.o `
            libsodium_crypto.c
          
          x86_64-w64-mingw32-ar.exe rcs `
            build/libsodium_crypto.lib `
            build/libsodium_crypto.o

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-mingw-libs
          path: |
            src/build/libsodium_crypto.dll
            src/build/libsodium_crypto.lib
